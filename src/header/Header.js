import React, { Component } from 'react'

const headerStyle = {
  textAlign: 'center',
  paddingTop: 5,
  paddingBottom: 70,
}

const Header = () => {
  return(
    <header style={headerStyle}>
      <h1>On Track</h1>
    </header>
  )
}

export default Header
