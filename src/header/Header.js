import React, { Component } from 'react'

const headerStyle = {
  textAlign: 'center',
  paddingTop: 15,
  paddingBottom: 50,
}

const Header = () => {
  return(
    <header style={headerStyle}>
      <h1>On Track</h1>
    </header>
  )
}

export default Header
